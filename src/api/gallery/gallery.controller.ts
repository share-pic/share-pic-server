import { Controller, Get, Param } from '@nestjs/common';

@Controller('gallery')
export class GalleryController {
  // @Get()
  // public getGalleryList(@Param('email') email: string): Promise<Gallery> {}
}
